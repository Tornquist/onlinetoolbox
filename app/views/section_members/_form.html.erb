<%= form_for(@section_member) do |f| %>
  <% if @section_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section_member.errors.count, "error") %> prohibited this section_member from being saved:</h2>

      <ul>
      <% @section_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.number_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :section_id %><br>
    <%= f.number_field :section_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
