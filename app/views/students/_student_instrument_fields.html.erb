<div class="field">
  <%= f.label :instrument_id %>
  <%= f.collection_select(:instrument_id, Instrument.all, :id, :name)  %>

  <%= f.label :ensemble_id %>
  <%= f.collection_select(:ensemble_id, Ensemble.all, :id, :name) %>
  <%= link_to_remove_association "remove", f, {wrapper_class: 'field'}%>
</div>
